# Sanitized Clover API samples

These files are **documentation fixtures only** — not live API responses from a merchant.

- No access tokens, authorization headers, or PII
- IDs are fictional (`SBX_*` prefix)
- Shapes derived from [Clover Platform API docs](https://docs.clover.com/dev/docs/expanding-fields) (Phase 2F.6)

Use for mapping triage when a live sandbox capture is unavailable. Re-validate against your sandbox merchant before production.
